# strategies/

Generated Hypothesis strategies, one file per agentic-loop iteration (`iteration_0_seed.py` through
`iteration_N.py`, plus `final.py`), each the literal code the LLM produced that round — not overwritten,
so the evolution across iterations (Step 4's "log of how it evolved") is reviewable file-by-file
alongside `logs/`.
